/**
 * @description These are the actions that a module can have. Actions other than
 * these are considered invalid.
 */

export const enum MODULE_ACTIONS {
	"READ" = "read",
	"CREATE" = "create",
	"UPDATE" = "update",
	"DELETE" = "delete",
}
